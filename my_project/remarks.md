Два важных момента:
1. Ожидалось использование библиотеки QAUtils для знакомства с ней. Под капотом там уже многое реализовано для упрощения разработки автотестов и уменьшения дублирования кода.
2. Отсутствует модуль/блок logic. Как я говорил ранее: в тестах мы не обращаемся к клиентам напрямую, они проходят через блок логики. 

Дополнительно: нет аннотаций allure, не сформировать понятный отчет.


Что хорошо: тесты построены по AAA-паттерну, проверки понятны и согласованы между собой (сначала проверяем что метод вообще вернул 200, далее проверяем состав ответа), проверяется то, что было в тест-кейсе.